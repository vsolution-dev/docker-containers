apm-server:
  host: 0.0.0.0:8200

output.elasticsearch:
  hosts:
    - ${ELASTICSEARCH_HOST}
  username: elastic
  password: ${ELASTIC_PASSWORD}

setup.kibana:
  host: ${KIBANA_HOST}
  username: kibana_system
  password: ${KIBANA_PASSWORD}
